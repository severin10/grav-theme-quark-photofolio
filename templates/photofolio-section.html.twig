{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="container">
        <p><a href="{{ page.parent.url }}">&larr; {{ page.parent.title }}</a></p>
        <h1 class="page-title">{{ page.title }}</h1>
    </div>
{% endblock %}
